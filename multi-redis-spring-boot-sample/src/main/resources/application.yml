server:
  port: 8080

spring:
  redis:
    auto-register: true
    clusters:
      order:
        host: localhost
        port: 6379
        password:
        database: 0
        lettuce:
          pool:
            max-active: 16
            max-idle: 8
            min-idle: 2
      user:
        host: localhost
        port: 6380
        password:
        database: 0
        lettuce:
          pool:
            max-active: 8
            max-idle: 4
            min-idle: 1
